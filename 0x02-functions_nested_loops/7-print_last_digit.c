#include"main.h"
#include<stdio.h>
/**
 * print_last_digit - prints the last digit of a number
 * @n: -character
 * Return: Always 0
 */
int print_last_digit(int n)
{
	int n;

	for (n < 0; n = 0; n > 0)
	{
	_putchar("n, n%10", n);
	return (0);
}
