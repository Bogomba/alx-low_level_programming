#include "main.h"
/**
 * main - check the code
 * Return: Always 0 (Success)
 */

int main(void)

{
	char lower;
	
	for (lower = 'a', lower <= 'z', lower++)	
	print_alphabet("%c"; lower);
	print_alphabet('\n');
	return (0);
	
}
