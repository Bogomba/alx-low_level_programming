#include"main.h"
/**
 * print_sign -  prints whether n is greater, equal to or less tha 0
 * @n: -character
 * Return: 1 if >0, 0 if =0, return -1 if <0
 */
int print_sign(int n)

{
	if (n > 0)
	_putchar('+');
	return (1);
	if else(n = 0)
	_putchar('0');
	return (0);
	else
	_puthcar('-');
	return (-1);
}
