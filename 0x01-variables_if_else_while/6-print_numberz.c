#include <stdio.h>
/**
 * main - Entry poin , print 0 to 9 without variables
 * Return: Always 0 (Success)
 */
int main(void)

{
	int n;

	if (n < 0)
	{
	putchar('-');
	n = -n;
	}

	; n <= 10; n++)
	putchar("%d, n");
	putchar("\n");
	return (0);
